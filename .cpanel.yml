---
deployment:
  tasks:
    - export DEPLOYPATH=/home1/gorkhal1/gorkhali-khabar/
    - /bin/mkdir -p "$DEPLOYPATH"
    - /usr/bin/rsync -a --delete --exclude='.git' --exclude='.env' --exclude='backend/.env' --exclude='node_modules' --exclude='.next' ./ "$DEPLOYPATH"
    - /bin/bash "$DEPLOYPATH/scripts/cpanel-deploy.sh"
